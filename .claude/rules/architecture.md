---
paths:
  - "knowit/**"
---

# Project map

`docs/architecture.md` has one line for each module in `knowit/`. When you add, remove, rename, or change
the role of a module, update that line in the same commit.
