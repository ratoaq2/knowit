---
paths:
  - "knowit/defaults.yml"
  - "knowit/config.py"
---

# Knowledge base

- A new codec, profile, or other known value is a change in `knowit/defaults.yml`, not in code.
- The section names are the `Configurable` class names. A renamed class needs a renamed section.
- The file format and how to add a value are in `docs/knowledge-base.md`.
